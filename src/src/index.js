import { render } from '@testing-library/react';
import React, { useState } from 'react';

function ReactNewComponent() {

    return (
        <div>
            <span>New data</span>
        </div>
    )
}